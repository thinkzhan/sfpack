{
  "name": "demo",
  "description": "sfpack demo",
  "scripts": {
    "dev": "sfpack --config=./build/sfpack.dev.config",
    "build": "sfpack --config=./build/sfpack.prod.config"
  },
  "devDependencies": {}
}
